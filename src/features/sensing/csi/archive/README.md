## CSI Archive

This directory stores frozen/experimental CSI modules that are intentionally
excluded from active runtime behavior.

- `CsiAlgorithm.cpp` remains disabled via `#if 0`.
- The code is preserved as an optimized ESP32 reference implementation for
  future reactivation and benchmarking.
